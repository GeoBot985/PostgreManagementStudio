# Sprint 55 shell consistency evidence

- Object Explorer context menu no longer launches an unrelated New Query action.
- Opening the tree context menu selects and focuses the item under the pointer,
  so subsequent context commands use the visible tree context rather than a
  later selection.
- Close-document command terminology is consistent across the File, Window and
  tab context routes.
- Performance Dashboard and Blocking Diagnostics remain canonical shared
  commands and focus the existing connection-scoped monitoring workspace.
- The shortcut inventory test reports no duplicate key/Modifier combinations.
- `STATE_OF_THE_NATION.md` was pre-existing and intentionally not modified.
