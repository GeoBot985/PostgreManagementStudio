# Handoff

Bootstrap and initial version-query slice created. Git and .NET 9.0.315 are installed; PostgreSQL is not installed, so live connection verification remains pending. Run restore/build/test and install PostgreSQL next.
